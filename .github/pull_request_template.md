## What changed

## Why it's needed

## How to test
1.
2.
3.

## Checklist
- [ ] Branched from `dev` — never from `main`
- [ ] Branch name follows convention: `feat/`, `fix/`, `db/`, `test/`, `docs/`, `chore/`
- [ ] All Vitest tests pass locally (`npm run test:run`)
- [ ] No `console.log` statements in production code
- [ ] No `.env` files or Supabase keys committed
- [ ] At least one team member has reviewed and approved
- [ ] Merge target is `dev` — never `main`
- [ ] Feature branch deleted after merge